<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorGreyDark</item>
        <!--<item name="colorPrimaryDark">@color/colorGrey</item>-->
    </style>

    <style name="textInputLayoutWhite" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@android:color/white</item>
        <item name="android:editTextColor">@android:color/white</item>
        <item name="colorAccent">@android:color/white</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@android:color/white</item>
    </style>

    <style name="textInputLayoutBlack" parent="TextAppearance.AppCompat">
        <item name="android:textColorHint">@android:color/black</item>
        <item name="android:editTextColor">@android:color/black</item>
        <item name="colorAccent">@android:color/black</item>
        <item name="colorControlNormal">@android:color/black</item>
        <item name="colorControlActivated">@android:color/black</item>
    </style>

    <style name="BottomNavigationTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/colorBlue</item>
        <item name="android:textColorSecondary">@color/colorGrey</item>
        <item name="android:textSize">10dp</item>
    </style>

    <style name="bottomNavigationElevation" parent="Theme.AppCompat">
        <item name="android:elevation">24dp</item>
    </style>

    <style name="customCalendarView">
        <item name="colorControlActivated">@color/Tale</item>
    </style>
</resources>
